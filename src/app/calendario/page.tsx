import type { Metadata } from 'next';
import Image from 'next/image';
import Link from 'next/link';
import { ArrowRight, CalendarDays, ChevronRight, Compass, Search, Sparkles } from 'lucide-react';
import Footer from '@/components/Footer';
import MarketplaceHeader from '@/components/MarketplaceHeader';
import CalendarEventIcon, { calendarKindStyles } from '@/components/CalendarEventIcon';
import SchoolCalendarMonth from '@/components/SchoolCalendarMonth';
import { getSchoolCalendarEventKinds, getSchoolCalendarEventsForMonth, SCHOOL_CALENDAR_ARTWORK, type SchoolCalendarEventKind } from '@/lib/school-calendar';
import { getAllPublicMarketplaceProducts } from '@/lib/store-service';

export const revalidate = 3600;
export const metadata: Metadata = { title: 'Calendário escolar e datas comemorativas | Educalizando', description: 'Planeje aulas com datas temáticas e encontre materiais do catálogo Educalizando por tema.', alternates: { canonical: '/calendario' }, openGraph: { title: 'Calendário escolar | Educalizando', description: 'Datas temáticas e materiais para apoiar o planejamento escolar.', url: '/calendario' }, twitter: { card: 'summary', title: 'Calendário escolar | Educalizando', description: 'Datas temáticas e materiais para apoiar o planejamento escolar.' } };

const MONTHS = ['janeiro', 'fevereiro', 'março', 'abril', 'maio', 'junho', 'julho', 'agosto', 'setembro', 'outubro', 'novembro', 'dezembro'];
function safeNumber(value: string | undefined, fallback: number, minimum: number, maximum: number) { const number = Number(value); return Number.isInteger(number) && number >= minimum && number <= maximum ? number : fallback; }
function calendarHref(month: number, year: number, type?: string) { const query = new URLSearchParams({ mes: String(month), ano: String(year) }); if (type) query.set('tipo', type); return `/calendario?${query.toString()}`; }

export default async function CalendarPage({ searchParams }: { searchParams: Promise<{ mes?: string; ano?: string; tipo?: string }> }) {
  const params = await searchParams; const now = new Date(); const month = safeNumber(params.mes, now.getMonth(), 0, 11); const year = safeNumber(params.ano, now.getFullYear(), 2020, 2100); const kinds = getSchoolCalendarEventKinds(); const activeType = kinds.includes(params.tipo as SchoolCalendarEventKind) ? params.tipo as SchoolCalendarEventKind : undefined; const allMonthEvents = getSchoolCalendarEventsForMonth(month); const events = activeType ? allMonthEvents.filter((event) => event.kind === activeType) : allMonthEvents;
  const products = await getAllPublicMarketplaceProducts(500); const materialCounts = new Map(allMonthEvents.map((event) => [event.slug, products.filter((product) => product.seasonal_tags?.includes(event.searchTerm)).length])); const selectedMonthIsCurrent = month === now.getMonth() && year === now.getFullYear(); const upcoming = [...allMonthEvents].filter((event) => !selectedMonthIsCurrent || event.day >= now.getDate()).slice(0, 3);

  return <div className="flex min-h-screen flex-col bg-slate-50"><MarketplaceHeader /><main className="flex-1"><div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 sm:py-10 lg:px-8">
    <nav aria-label="Breadcrumb" className="flex flex-wrap items-center gap-1 text-sm font-semibold text-slate-600"><Link href="/" className="rounded hover:text-blue-700 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600">Início</Link><ChevronRight aria-hidden="true" className="h-4 w-4" /><span aria-current="page" className="text-slate-900">Calendário</span></nav>
    <header className="home-calendar-panel relative isolate mt-6 overflow-hidden rounded-[2rem] border border-blue-100 bg-gradient-to-br from-blue-800 via-blue-700 to-cyan-600 p-6 text-white shadow-xl shadow-blue-950/15 sm:p-10">
      <div aria-hidden="true" className="pointer-events-none absolute inset-0 overflow-hidden"><div className="absolute -right-12 -top-16 h-56 w-56 rounded-full bg-cyan-200/25 blur-3xl" /><div className="absolute -bottom-20 left-1/3 h-48 w-48 rounded-full bg-violet-300/20 blur-3xl" /><div className="absolute bottom-0 right-12 h-24 w-24 rotate-12 rounded-3xl border border-white/20 bg-white/10" /><span className="home-calendar-float absolute right-[8%] top-8 text-5xl drop-shadow-lg">📚</span><span className="home-calendar-float-delayed absolute bottom-8 right-[21%] text-4xl opacity-85">🌳</span><span className="home-calendar-sparkle absolute right-[34%] top-12 text-2xl text-yellow-100">✦</span><span className="home-calendar-sparkle-delayed absolute right-[12%] top-1/2 text-xl text-white">✧</span></div>
      <div className="relative grid gap-6 lg:grid-cols-[minmax(0,1fr)_15rem]"><div><p className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/10 px-3 py-1.5 text-xs font-black uppercase tracking-[.16em] text-cyan-100 backdrop-blur-sm"><CalendarDays aria-hidden="true" className="h-4 w-4" /> Planejamento pedagógico</p><h1 className="mt-3 max-w-3xl text-3xl font-black leading-tight sm:text-5xl">Calendário escolar para planejar com mais contexto</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-blue-50 sm:text-base">Consulte datas temáticas, organize ideias e acesse materiais que os criadores marcaram diretamente no catálogo.</p><div className="mt-6 flex flex-col gap-3 sm:flex-row"><Link href="#datas-do-mes" className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-white px-5 text-sm font-black text-blue-800 shadow-lg shadow-blue-950/15 transition hover:-translate-y-0.5 hover:bg-blue-50 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">Ver datas deste mês <ArrowRight aria-hidden="true" className="h-4 w-4" /></Link><Link href="/buscar" className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl border border-white/30 px-5 text-sm font-black text-white transition hover:-translate-y-0.5 hover:bg-white/10 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"><Search aria-hidden="true" className="h-4 w-4" />Explorar materiais</Link></div></div><div className="hidden lg:flex items-center justify-center"><div className="grid h-40 w-40 place-items-center rounded-[2rem] border border-white/20 bg-white/10 shadow-inner backdrop-blur-sm"><CalendarDays aria-hidden="true" className="home-calendar-float h-20 w-20 text-cyan-100" /></div></div></div>
    </header>
    <section aria-label="Filtrar datas" className="home-calendar-filter-panel relative mt-6 overflow-hidden rounded-3xl border border-slate-200 bg-white p-4 shadow-sm"><div aria-hidden="true" className="pointer-events-none absolute inset-0"><span className="home-calendar-sparkle absolute right-[6%] top-2 text-lg text-violet-300">✦</span><span className="home-calendar-sparkle-delayed absolute bottom-2 right-[34%] text-sm text-cyan-300">✧</span></div><div className="relative flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="inline-flex items-center gap-1.5 text-[10px] font-black uppercase tracking-[.14em] text-blue-700"><Sparkles aria-hidden="true" className="h-3.5 w-3.5" /> Planeje por tema</p><h2 className="mt-1 text-sm font-black text-slate-900">Filtre pelo tipo de tema</h2><p className="mt-1 text-xs text-slate-500">A filtragem mantém o mês selecionado.</p></div><div className="flex flex-wrap gap-2"><Link href={calendarHref(month, year)} aria-current={!activeType ? 'page' : undefined} className={`inline-flex min-h-10 items-center rounded-full px-4 text-xs font-black transition hover:-translate-y-0.5 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 ${!activeType ? 'bg-blue-700 text-white shadow-md shadow-blue-700/20' : 'bg-slate-100 text-slate-700 hover:bg-blue-50'}`}>Todos</Link>{kinds.map((kind, index) => { const style = calendarKindStyles[kind]; return <Link key={kind} href={calendarHref(month, year, kind)} aria-current={activeType === kind ? 'page' : undefined} className={`group inline-flex min-h-10 items-center gap-2 rounded-full px-4 text-xs font-black ring-1 transition hover:-translate-y-0.5 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600 ${activeType === kind ? 'bg-blue-700 text-white ring-blue-700 shadow-md shadow-blue-700/20' : `${style.chip} hover:brightness-95`}`}><span className={`home-calendar-filter-dot h-2 w-2 rounded-full ${activeType === kind ? 'bg-white' : style.dot}`} style={{ animationDelay: `${index * 180}ms` }} aria-hidden="true" />{kind}</Link>; })}</div></div></section>
    <div className="mt-7 grid gap-6 lg:grid-cols-[minmax(0,1.15fr)_minmax(18rem,.85fr)]"><SchoolCalendarMonth month={month} year={year} events={events} materialCounts={materialCounts} activeType={activeType} /><aside className="rounded-[2rem] border border-indigo-100 bg-indigo-50 p-5 shadow-sm sm:p-6"><p className="inline-flex items-center gap-2 text-xs font-black uppercase tracking-[.14em] text-indigo-700"><Sparkles aria-hidden="true" className="h-4 w-4" /> Próximas datas</p><h2 className="mt-2 text-2xl font-black capitalize text-slate-950">Em {MONTHS[month]}</h2>{upcoming.length ? <ul className="mt-5 space-y-3">{upcoming.map((event) => { const count = materialCounts.get(event.slug) || 0; const style = calendarKindStyles[event.kind]; return <li key={event.slug}><Link href={`/calendario/${event.slug}`} className="group flex min-h-20 items-center gap-3 rounded-2xl border border-white bg-white p-3 shadow-sm hover:border-indigo-200 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-700"><span className={`grid h-11 w-11 shrink-0 place-items-center rounded-xl ${style.icon}`}><CalendarEventIcon icon={event.icon} className="h-5 w-5" /></span><span className="min-w-0 flex-1"><span className="block text-xs font-bold text-slate-500">{event.day} de {MONTHS[month]}</span><span className="block font-black text-slate-900 group-hover:text-indigo-800">{event.name}</span>{count > 0 && <span className="mt-1 block text-xs font-bold text-emerald-700">Materiais marcados no catálogo</span>}</span><ChevronRight aria-hidden="true" className="h-4 w-4 shrink-0 text-slate-400" /></Link></li>; })}</ul> : <p role="status" className="mt-5 rounded-2xl border border-dashed border-indigo-200 bg-white p-4 text-sm leading-6 text-slate-600">Não há próximas datas nesta seleção para este mês.</p>}</aside></div>
    <section id="datas-do-mes" aria-labelledby="titulo-datas" className="mt-8 scroll-mt-28"><div className="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-black uppercase tracking-[.14em] text-blue-700">Lista acessível de planejamento</p><h2 id="titulo-datas" className="mt-1 text-2xl font-black capitalize text-slate-950 sm:text-3xl">Datas de {MONTHS[month]}</h2></div><p aria-live="polite" className="text-sm font-semibold text-slate-600">{events.length} {events.length === 1 ? 'tema nesta seleção' : 'temas nesta seleção'}</p></div>{events.length ? <div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{events.map((event) => { const count = materialCounts.get(event.slug) || 0; const style = calendarKindStyles[event.kind]; const artwork = SCHOOL_CALENDAR_ARTWORK[event.slug]; return <article key={event.slug} className="flex min-w-0 flex-col overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm">{artwork && <div className="relative h-36 overflow-hidden bg-slate-100"><Image src={artwork.src} alt={artwork.alt} fill sizes="(min-width: 1280px) 24rem, (min-width: 640px) 45vw, 100vw" className="object-cover object-right" /></div>}<div className="flex flex-1 flex-col p-5"><div className="flex items-start gap-3"><span className={`grid h-11 w-11 shrink-0 place-items-center rounded-xl ${style.icon}`}><CalendarEventIcon icon={event.icon} /></span><div className="min-w-0"><p className="text-sm font-black text-blue-700">{event.day} de {MONTHS[month]}</p><h3 className="mt-1 break-words text-lg font-black leading-tight text-slate-950">{event.name}</h3></div></div><span className={`mt-4 inline-flex w-fit rounded-full px-3 py-1 text-xs font-bold ring-1 ${style.chip}`}>{event.kind}</span><p className="mt-3 text-sm leading-6 text-slate-600">{event.description}</p><div className="mt-auto grid gap-2 pt-5"><Link href={`/calendario/${event.slug}`} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl border border-blue-200 px-4 text-sm font-black text-blue-800 hover:bg-blue-50 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600">Ver detalhes <ChevronRight aria-hidden="true" className="h-4 w-4" /></Link><Link href={`/buscar?data=${encodeURIComponent(event.searchTerm)}`} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-blue-700 px-4 text-sm font-black text-white hover:bg-blue-800 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600">{count > 0 ? 'Ver materiais marcados' : 'Buscar materiais sobre o tema'} <Search aria-hidden="true" className="h-4 w-4" /></Link></div></div></article>; })}</div> : <div role="status" className="mt-5 rounded-3xl border border-dashed border-slate-300 bg-white p-7 text-center"><h3 className="text-lg font-black text-slate-900">Nenhuma data deste tipo neste mês</h3><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-slate-600">Tente outro tipo de tema ou volte para a lista completa de {MONTHS[month]}.</p><Link href={calendarHref(month, year)} className="mt-4 inline-flex min-h-11 items-center rounded-xl bg-blue-700 px-5 text-sm font-black text-white hover:bg-blue-800 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600">Mostrar todas as datas</Link></div>}</section>
    <section className="mt-9 rounded-[2rem] border border-emerald-100 bg-gradient-to-br from-emerald-50 to-cyan-50 p-6 sm:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-black text-slate-950">Como usar este calendário</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-600">Escolha uma data, confira o tema e use a busca para encontrar materiais já marcados pelos criadores. Quando não houver marcação, a busca temática continua disponível.</p></div><Link href="/buscar" className="inline-flex min-h-11 shrink-0 items-center justify-center gap-2 rounded-xl bg-emerald-700 px-5 text-sm font-black text-white hover:bg-emerald-800 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-emerald-700"><Compass aria-hidden="true" className="h-4 w-4" />Explorar catálogo</Link></div></section>
  </div></main><Footer /></div>;
}
